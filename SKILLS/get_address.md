# get_address

Retrieve a single Address element by ID.

## Endpoint

`get_address`

## Parameters

- `addressId` (int, required) - Address element ID

## Returns

Returns the address record with owner information, linked address field info, and custom field values.

## Example

```json
{
  "addressId": 42
}
```
